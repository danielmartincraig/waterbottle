(ns com.danielmartincraig.waterbottle-test
  (:require [clojure.test :refer :all]
            [com.danielmartincraig.waterbottle :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
